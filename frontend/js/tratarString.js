// export function prepararStringParaBusca(stringRecebida) {
//     // Remove acentos
//     const normalizedStr = stringRecebida.normalize("NFD").replace(/[\u0300-\u036f]/g, "");

//     // Remove caracteres especiais
//     const filteredStr = normalizedStr.replace(/[^\w\s]/g, "");

//     // Transforma em minúsculas
//     const lowercasedStr = filteredStr.toLowerCase();

//     // Remove espaços em branco
//     const trimmedStr = lowercasedStr.trim();

//     return trimmedStr;
// }

// teste